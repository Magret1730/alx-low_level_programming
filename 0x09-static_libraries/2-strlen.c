#include "main.h"

/**
 * _strlen - string length
 * @s: takes in characters
 *
 * Description: a function that returns the
 * length of a string
 * Return: i, length of string
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}

	return (i);
}
